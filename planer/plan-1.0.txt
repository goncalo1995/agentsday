Based on my éFacil pre-build (auth, Viator manual search, short URLs, click logging), upgrade it to MVP.

Add OpenRouter-powered AI intent extraction:

1. Page: "Describe your audience's travel intent"
   - User inputs free text (e.g., "bachelorette party in Nashville with party bus and bar crawl")
2. Call OpenRouter (use gpt-3.5-turbo or gemini-2.0-flash) with a system prompt that:
   - Extracts: destination, budget_per_person, keywords, group_type, datetime constraints.
   - Outputs JSON only.
3. Use extracted fields to call Viator sandbox search (map keywords to Viator categories).
4. Rank results: match score (ai_keywords + destination + price range).
5. Show top 3 activities → user picks one → generate Viator affiliate link + short URL.
6. Dashboard shows:
   - List of generated links
   - Clicks per link (from my existing click logging)
   - Estimated commission (assume 8% of Viator price)

Handle fallback: if AI fails or zero results, show manual search as fallback (my pre-build UI).

Do not break my existing manual search flow. Keep Viator sandbox mode.
🔁 V2 (Analytics + Payout Readiness – 3–4 weeks)
Goal: Influencers see which content drives clicks + you can track commissions without live webhooks.

What to ship (from blueprint)
7.4: Creator analytics frontend (clicks over time, by activity, by destination)

8.2: Commission ledger (manual CSV/API upload from Viator while sandbox)

6.4 + 6.5: Link management + one-click share (copy link, Instagram bio ready)

5.4: Better ranking (click-through rate boosting)

Bot filtering (Cloudflare or simple user-agent filter)

📋 Codex Prompt (V2)
text
Upgrade éFacil from MVP to V2.

Add these features without breaking existing AI intent + link generation:

1. Analytics Dashboard (per creator):
   - Total clicks (last 7, 30 days)
   - Clicks per activity (bar chart)
   - Clicks per destination
   - Time series (daily clicks)
   - Estimated commissions (8% fixed for now, but store configurable)

2. Commission Ledger:
   - Admin page to upload Viator sandbox CSV (or manual entry) for booked/paid commissions.
   - Link bookings back to clicks using click_id + timestamp range.
   - Show "pending" vs "confirmed" commission.

3. Bot filtering:
   - Skip logs for user-agent containing "bot/crawler/headless"
   - Keep raw clicks but separate "filtered_clicks" table.

4. Link Management:
   - Edit link title / notes
   - Deactivate link
   - One-click copy short URL and "Instagram share text" (generated by OpenRouter: 2 sentences + 3 emojis)

Keep using Viator sandbox. Keep OpenRouter intent extraction. Output migration SQL and new/modified files.
📈 V3 (Near Blueprint – 6–8 weeks)
Goal: Multi-provider (GetYourGuide, etc.) + automated payouts + influencer waitlist.

What to ship (blueprint leftovers)
8.1 + 8.3: Provider webhook receiver (Viator postback simulation)

5.5: No-results → hybrid AI → email alert

9.4 + 9.5: Full responsive + loading/error/empty states

11.3 + 12.4: Onboarding email + feedback collection

Second provider (GetYourGuide or Tiqets) to show extensibility

📋 Codex Prompt (V3)
text
Upgrade éFacil from V2 to V3 – near blueplat complete.

Deliver:

1. Multi-provider support:
   - Add GetYourGuide API (sandbox if available).
   - AI intent extraction decides preferred provider based on destination/activity type.
   - Unify "generate link" into one UI (provider auto-detected or user chooses).

2. Webhook receiver (simulated for now):
   - Endpoint POST /api/webhooks/viator (accepts sandbox test payloads).
   - Update commission ledger from "estimated" to "confirmed" when webhook says "booked".

3. No-results fallback:
   - If AI search returns <1 result, offer creator to:
     a. Launch manual search (existing)
     b. Request human curation (email to admin)
   - Show "suggest alternative destination" using OpenRouter.

4. Full frontend polish:
   - Responsive (mobile-first)
   - Loading skeletons on search/AI
   - Empty states (no clicks yet, no links yet)
   - Error boundaries + retry buttons

5. Creator onboarding email sequence (triggered after signup):
   - Welcome → How to create first link → Analytics tips.

Do not remove Viator sandbox. Keep OpenRouter as the AI layer. Include migration for provider field in links table.
🧠 Tips for Success with Codex
Version	Most critical prompt section	Why
Pre-build	“Keep modular so I can add AI later”	Avoids rewriting later
MVP	“Output JSON only” (OpenRouter)	Makes parsing reliable
V2	“Bot filtering”	Prevents fake clicks from ruining tests
V3	“Unify provider UI”	Keeps influencer experience simple
Also always append to your prompts:

“Do not refactor code that works. Prefer additive changes unless explicitly stated otherwise.”